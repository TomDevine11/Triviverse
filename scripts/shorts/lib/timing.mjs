// Central timing knobs for the short-form videos. Change a number here and re-render — this is the
// single place to tune the opening card and every countdown, since those are common requests.
export const INTRO_CARD = 3.7          // seconds — opening "direct question" card shown on EVERY video
export const FLOW_COUNTDOWN = 6.0      // who-am-i / career-path / guess-the-club / played-alongside
export const POINTLESS_COUNTDOWN = 10.0 // football-pointless / player-between-clubs (think time)
export const WINNER_COUNTDOWN = 5.0    // winner-stays-on (per-round decision time)
